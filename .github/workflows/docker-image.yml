name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Docker Buildx
      id: buildx
      uses: docker/setup-buildx-action@v2
    - name: Build the Docker image for terraria-docker
      run: docker build . --file Dockerfile --tag cfindlayisme/terraria-docker:latest
    - name: Build the image for terraria-docker-gcp
      run: docker build . --file Dockerfile-GCP --tag cfindlayisme/terraria-docker-gcp:latest
    - name: Login to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    - name: Push the Docker image for terraria-docker
      run: docker push cfindlayisme/terraria-docker:latest
    - name: Push the image for terraria-docker-gcp
      run: docker push cfindlayisme/terraria-docker-gcp:latest
    
